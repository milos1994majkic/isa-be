version: '3.8'

services:
  db:
    image: postgres:latest
    restart: always
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: isa-database
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: admin
